{% extends 'BackendBundle:Default:layout.html.twig' %}

    {% block content %}
        <div class="row">
            <div class="col-xs-12">
                <div class="box">
                    <div class="box-header">
                        <h3 class="box-title">Editar Local</h3>
                        <p>
                            <a href="{{ path('administration_Location') }}">
                                <span class="glyphicon glyphicon-back"></span>Voltar
                            </a>
                        </p>
                    </div>
                    <!-- /.box-header -->

                    <div class="box-body">
                        {{ include('BackendBundle:Location:form.html.twig', {'form' : edit_form}) }}
                    </div>
                    <!-- /.box-body -->
                </div>
                <!-- /.box -->
            </div>
        </div>
    {% endblock %}
