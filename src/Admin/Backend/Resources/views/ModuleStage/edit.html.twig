{% extends 'BackendBundle:Default:layout.html.twig' %}

    {% block content %}
        <div class="row">
            <div class="col-xs-12">
                <div class="box">
                    <div class="box-header">
                        <h3 class="box-title">Editar ModuleStage</h3>
                        <p>
                            <a href="{{ path('administration_ModuleStage') }}">
                                <span class="glyphicon glyphicon-back"></span> Voltar
                            </a>
                        </p>
                    </div>
                    <!-- /.box-header -->

                    <div class="box-body no-padding">
                        {{ form(edit_form) }}
                    </div>
                    <!-- /.box-body -->
                </div>
                <!-- /.box -->
            </div>
        </div>
    {% endblock %}
