{% extends 'BackendBundle:Default:layout.html.twig' %}

    {% block content %}
        <div class="row">
            <div class="col-xs-12">
                <div class="box">
                    <div class="box-header">
                        <h3 class="box-title">
                            <center>
                                <strong>CORREÇÃO, AÇÕES CORRETIVAS E AÇÕES PREVENTIVAS</strong>
                            </center>
                        </h3>

                        <p style="font-size:12px;color:gray">
                            <span class="fa fa-user"></span>
                            Criada por {{entity.createdBy.name}}/{{entity.createdBy.entity.name}}
                        </p>

                        <p>
                            <a href="{{ path('administration_IReclamation') }}">                        
                                <span class="fa fa-back"></span>Voltar
                            </a>
                        </p>
                    </div>
                    <!-- /.box-header -->

                    <div class="box-body">
                    {{ include('BackendBundle:Correction:form.html.twig', {'form' : edit_form}) }}
                    </div>
                    <!-- /.box-body -->
                </div>
                <!-- /.box -->
            </div>
        </div>
    {% endblock %}
